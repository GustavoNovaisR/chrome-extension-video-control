// import { SenderMessage } from "../dir_shared/interfaces/SenderMessage.interface";

console.log('coming soon.....');


(() => {

    // const iframesUrls = new Set<string>()
    // const allTagsIframe = (<HTMLIFrameElement[]><any>document.getElementsByTagName('source'))

    // for (const iframe of allTagsIframe) {
    //     iframesUrls.add(iframe.src)
    // }

    // if (iframesUrls.size > 0) {
    //     const message: SenderMessage = {
    //         type: 'iframesRequest',
    //         payload: [...iframesUrls],
    //     }
    //     chrome.runtime.sendMessage(message);
    // }

})()